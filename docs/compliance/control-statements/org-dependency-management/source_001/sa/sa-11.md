---
x-trestle-comp-def-rules:
  org-dependency-management:
    - name: renovate-onboarded
      description: The system repo has merged the Renovate onboarding PR and extends
        the org preset
    - name: actions-sast-scanned
      description: GitHub Actions workflows are scanned for security issues via centralized
        zizmor SAST analysis
---

# sa-11 - \[SA\] Developer Testing and Evaluation

## Control Statement

Require the developer of the system, system component, or system service to create and implement a security and privacy assessment plan; perform unit, integration, system, and regression testing/evaluation; produce evidence of the execution of the plan and the results; and implement a verifiable flaw remediation process.

## What is the solution and how is it implemented?

### org-dependency-management

#### Part a

PR-based updates ensure that CI tests run against every dependency version change before it reaches the default branch. Merge decisions remain with each team, who are responsible for their own test quality. The PR review gate enforces that no dependency change bypasses the test pipeline.

The org preset intentionally does NOT enable auto-merge centrally — each team decides based on their test coverage and risk tolerance, maintaining accountability for test adequacy at the system level.

Centralized SAST analysis provides automated static security testing of GitHub Actions workflow definitions, scanning for template injection, credential exposure, excessive permissions, and other security anti-patterns. Auto-fixable findings are remediated via PR; unfixable findings are reported via issue for manual review. This runs weekly across all org repos — no per-repo setup required.

#### Rules:

  - renovate-onboarded
  - actions-sast-scanned

#### Implementation Status: implemented
